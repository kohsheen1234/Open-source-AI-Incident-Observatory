server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Single-page app: fall back to index.html for client routes.
    location / {
        try_files $uri /index.html;
    }
}
